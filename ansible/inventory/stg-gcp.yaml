---
plugin: gcp_compute
auth_kind: application
projects:
  - kubernetes-istio-gcp
filters:
  - labels.ansible != ""
keyed_groups:
  - key: labels.ansible
    separator: ''
compose:
  # set the ansible_host variable to connect with the private IP address without changing the hostname
  ansible_host: name